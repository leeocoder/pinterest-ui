import { theme } from '@/themes';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  flatList: {
    paddingBottom: 4,
    maxHeight: 32,
  },
  content: {
    gap: 24,
    paddingHorizontal: 8,
  },
});
